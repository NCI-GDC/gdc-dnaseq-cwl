#!/usr/bin/env cwl-runner

cwlVersion: v1.0

requirements:
  - class: DockerRequirement
    dockerPull: quay.io/jeremiahsavage/picard
  - class: InlineJavascriptRequirement

class: CommandLineTool

inputs:
  - id: INPUT
    type: File
    format: "edam:format_2572"
    inputBinding:
      prefix: "INPUT="
      separate: false

outputs:
  - id: OUTPUT
    type: File
    outputBinding:
      glob: $(inputs.INPUT.nameroot + ".validate")

arguments:
  - valueFrom: "MAX_OUTPUT=2147483647"

  - valueFrom: "MODE=VERBOSE"

  - valueFrom: $(inputs.INPUT.nameroot + ".validate")
    prefix: "OUTPUT="
    separate: false

  - valueFrom: "VALIDATION_STRINGENCY=LENIENT"
  
baseCommand: [java, -jar, /usr/local/bin/picard.jar, ValidateSamFile]
